# When Detox (or CI) builds with EXPO_PUBLIC_E2E=true, embed the JS bundle for Debug
# so env-driven flags are baked in. Default Debug keeps SKIP_BUNDLING=1 for Metro.
if [[ "${EXPO_PUBLIC_E2E:-}" == "true" ]]; then
  unset SKIP_BUNDLING
fi
