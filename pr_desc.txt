🎯 **What:** The testing gap for the `RateLimiter` class in `utils/rate-limiter.ts` was addressed. The class had well-defined logic for rate limiting attempts within a time window, but no tests existed.
📊 **Coverage:** A new test file `utils/__tests__/rate-limiter.test.ts` was created using Jest. It covers:
- Initial state and successful attempts within the limit
- Exceeding the maximum limit (`maxAttempts`)
- Independent limit tracking for different identifiers
- Window expiration (`windowMs`) and resets using `jest.useFakeTimers()`
- Remaining attempts calculation (`getRemainingAttempts`)
- Reset time calculation (`getResetTime`)
- Manual resets (`reset`)
- Periodic cleanup of expired entries (`cleanup`)
✨ **Result:** The `RateLimiter` class now has full test coverage, ensuring its behavior is deterministic and protected against future regressions.
